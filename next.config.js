const withImages = require('next-images')
module.exports = withImages()
//With aditional configurations below
// const withImages = require('next-images')
// module.exports = withImages({
//     webpack(config, options) {
//         return config
//     }
// })
